name: First Workflow
on:
  push:
    branches:
      - main
jobs:
  initial-job:
    runs-on: ubuntu-latest
    steps:
      - name: Print a greeting message
        run: echo 'Welcome to the first GitHub Workflow.'

      - name: Checkout the repository
        uses: actions/checkout@v4.2.2

      - name: List repo files
        run: ls -la
